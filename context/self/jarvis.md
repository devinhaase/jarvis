# Jarvis — Self-Knowledge

_Auto-generated by `generate_self_knowledge.py` — do not hand-edit, changes will be
overwritten. Regenerate with `python generate_self_knowledge.py`. Last generated: 2026-08-08 04:55 UTC._

## What I am

I'm Jarvis, Devin's personal AI agent — not a general-purpose chatbot. I run as a
single long-lived server process (`server.py`) on Devin's own machine, reachable from
multiple devices (desktop GUI, CLI, voice companion) over a shared WebSocket protocol, with
conversation history, projects, and file uploads persisted to a local SQLite database
(`data/jarvis.db`). Nothing about me runs in the cloud except the LLM API calls themselves
(and even those are optional — I run fully offline against a local Ollama model with zero
API keys configured).

I operate under a tiered approval system for everything I do (Tier 1 read-only through
Tier 4 explicit-confirmation-every-time), a security-specialist mode with its own
offense/defense/practice taxonomy and a hard authorization allowlist for anything that
touches a network target that isn't Devin's own machine, and — as of Phase 8 — a file-based
kill switch that immediately halts every Tier 2+ action from any connected device.

## Tool inventory

### General (assistant/ops) (30)

| Tool | Tier | Local-machine? | Description |
|---|---|---|---|
| `add_task` | TIER_2 |  | Add a task/reminder (title, optional details, optional due_date as YYYY-MM-DD) |
| `arm_kill_switch` | TIER_1 |  | EMERGENCY STOP: immediately refuse every Tier 2+ tool call until disarmed. Takes an optional 'reason'. Always Tier 1 itself — stopping must never be blocked by anything. |
| `arp_table_snapshot` | TIER_1 | yes | Read the local ARP table for a device inventory (IP + MAC pairs) — diffable across calls to spot a new device joining |
| `bandwidth_sample` | TIER_1 | yes | Sample this machine's network interface bytes sent/received over a short window via psutil, report an approximate throughput |
| `check_latency` | TIER_1 | yes | Ping a host (default 8.8.8.8) N times and report min/avg/max latency and packet loss |
| `check_system_health` | TIER_1 | yes | Check system CPU usage |
| `check_wan_status` | TIER_1 | yes | Query the router's UPnP/IGD service (if enabled) for external IP and WAN byte counters — degrades gracefully if UPnP isn't reachable |
| `complete_task` | TIER_2 |  | Mark a task complete by its id |
| `create_backup` | TIER_2 | yes | Create a local timestamped zip backup of data/ (conversations, memory, authorized targets) plus .env/devices.json/credentials — never uploaded anywhere. Prunes old backups. |
| `create_team_incident` | TIER_2 |  | File a finding on the shared team incident board for another team to pick up (target_team=null broadcasts to any team's queue). This is how a Network or IT finding reaches the Cybersecurity team without going through Devin each time. |
| `delete_task` | TIER_2 |  | Delete a task by its id |
| `diagnose_connectivity` | TIER_1 | yes | Combine ping sweep, latency, ARP inventory, and WAN status into a synthesized connectivity diagnosis with suggested next steps (diagnosis only — never changes live config) |
| `disarm_kill_switch` | TIER_2 |  | Turn the kill switch back off, resuming normal tool execution. |
| `draft_local_note` | TIER_2 | yes | Draft a markdown note |
| `error_tool` | TIER_1 |  | Forces an error for testing |
| `get_self_knowledge` | TIER_1 |  | Return a live self-knowledge document about Jarvis itself: full tool inventory by tier/role, current security posture, test coverage, recent capability history, and known scope boundaries. Call this when Devin asks what you can do, how you're built, or what your limits are — don't guess from training data. |
| `get_usage_stats` | TIER_1 |  | Approximate LLM token usage and cost over the last N days (default 30), broken down by provider — always $0 on Ollama (local), only matters if a paid API is active |
| `kill_switch_status` | TIER_1 |  | Check whether the kill switch is currently armed and why. |
| `list_directory` | TIER_1 | yes | List a local directory's immediate contents (name, is_dir, size_bytes) |
| `list_tasks` | TIER_1 |  | List tasks, soonest-due first — undone tasks by default, pass include_completed=true for everything |
| `list_team_incidents` | TIER_1 |  | List items on the shared team incident board — optionally filtered by target_team and/or status (open/acknowledged/resolved). |
| `move_or_rename_path` | TIER_3 | yes | Move or rename a local file/directory — refuses if the destination already exists |
| `ping_sweep` | TIER_1 | yes | ICMP ping sweep of the local subnet (auto-detected from the real LAN adapter) — returns which hosts respond |
| `read_local_file` | TIER_1 | yes | Read a local text file's content (truncated to max_chars) |
| `read_recent_emails` | TIER_1 |  | Fetch unread emails via Gmail API |
| `run_local_script` | TIER_3 | yes | Run an arbitrary local script or command |
| `scan_local_logs` | TIER_1 | yes | Scan Windows event logs for errors |
| `semantic_search_conversations` | TIER_1 |  | Search past conversations by meaning, not just keyword (complements the GUI's keyword search) — finds conceptually related conversations even if the wording differs entirely. Degrades gracefully if the local embedding model isn't available. |
| `web_search` | TIER_1 |  | General-purpose web search (not security-scoped) — DuckDuckGo by default, no API key required; uses Brave Search if BRAVE_SEARCH_API_KEY is set |
| `write_local_file` | TIER_2 | yes | Write or append to a local text file |

### Defense (6)

| Tool | Tier | Local-machine? | Description |
|---|---|---|---|
| `check_credential_hygiene` | TIER_1 | yes | Analyze your local Bitwarden vault for reused/breached passwords via the bw CLI (requires BW_SESSION; Jarvis never touches your master password) |
| `check_dependency_vulnerabilities` | TIER_1 | yes | Check a local requirements.txt's exactly-pinned packages against known CVEs via OSV.dev (defaults to this project's own requirements.txt) |
| `check_email_breach` | TIER_1 |  | Check if an email address appears in known data breaches via HaveIBeenPwned API |
| `check_password_breach` | TIER_1 |  | Check if a password has appeared in known breaches via k-anonymity API (password never sent) |
| `get_security_posture` | TIER_1 | yes | Check local machine security posture: Defender, firewall, patches, listening services |
| `run_self_audit` | TIER_1 | yes | Read-only audit of Jarvis's own security hardening: kill switch state, whether credential files are accidentally tracked in git, registered device count/staleness. |

### Offense (authorization-gated) (12)

| Tool | Tier | Local-machine? | Description |
|---|---|---|---|
| `check_ssl_cert` | TIER_1 | yes | Check an authorized host's TLS certificate: issuer, validity window, days until expiry |
| `dns_recon` | TIER_1 | yes | Resolve A/AAAA/MX/TXT/NS/CNAME records for an authorized domain via nslookup |
| `generate_payload` | TIER_4 | yes | Generate an attack payload via msfvenom for an authorized target — writes a file only, never executes/deploys it (Tier 4: requires explicit confirmation) |
| `http_security_headers_audit` | TIER_1 |  | Audit an authorized URL for standard defensive HTTP headers (HSTS, CSP, X-Frame-Options, ...) — same checks as securityheaders.com |
| `run_exploit_module` | TIER_4 | yes | Run one exact, named Metasploit exploit module against an authorized target, one shot, no session chaining (Tier 4: requires explicit confirmation) |
| `run_privesc_enum` | TIER_1 | yes | Run privilege escalation enumeration on the local machine (always authorized, local only) |
| `run_recon_pipeline` | TIER_3 | yes | Full recon pipeline (host discovery -> port scan -> service ID -> CVE lookup) against an authorized target only |
| `scan_ports` | TIER_3 | yes | Scan open ports on an authorized host (nmap required) |
| `shodan_lookup` | TIER_1 |  | Look up an authorized IP/domain on Shodan (passive, requires SHODAN_API_KEY, still gated by authorized_targets.json) |
| `subdomain_enum` | TIER_1 |  | Passively enumerate subdomains for an authorized domain via public certificate-transparency logs (crt.sh) — nothing sent to the target itself |
| `tech_fingerprint` | TIER_1 |  | Passively fingerprint the tech stack (server, CMS, JS frameworks) of an authorized URL from one GET request |
| `whois_lookup` | TIER_1 | yes | WHOIS registration lookup for an authorized domain/IP (requires a local whois client) |

### Practice / Reference (13)

| Tool | Tier | Local-machine? | Description |
|---|---|---|---|
| `cert_study_session` | TIER_1 |  | Generate a study session context for a security certification (CEH, OSCP, Security+, etc.) |
| `crack_hash` | TIER_2 | yes | Attempt to crack a hash via hashcat/john for a CTF or practice lab. REQUIRES a 'source' arg of 'ctf' or 'practice' — refuses otherwise. |
| `ctf_assistant` | TIER_1 |  | Analyze a CTF challenge description and return category + methodology context for the LLM |
| `exploitdb_usage` | TIER_1 |  | Fetch the published usage notes/source for a specific Exploit-DB entry by ID — read-only, same public content search_exploitdb links to |
| `generate_pentest_report` | TIER_2 | yes | Generate a markdown pentest report from recon results/findings, saved to data/pentest_reports/ |
| `get_security_reference` | TIER_1 |  | Get educational reference on ethical hacking, CTF, or security certification topics |
| `identify_hash` | TIER_1 |  | Identify a hash's type from its length/format (no cracking, informational only) |
| `lookup_cve` | TIER_1 |  | Search NIST NVD for CVEs matching a service/version string (public API, read-only) |
| `mitre_attack_lookup` | TIER_1 |  | Look up a MITRE ATT&CK tactic or technique by name, ID, or keyword (curated reference set) |
| `msf_module_info` | TIER_1 | yes | Read-only: search Metasploit modules matching a term and return the top match's documentation — no module is run |
| `owasp_top10_reference` | TIER_1 |  | Look up an OWASP Top 10 (2021) category — example, impact, mitigation. No topic returns all ten. |
| `reverse_shell_cheatsheet` | TIER_1 |  | Static reference: common reverse-shell one-liners (bash/python/perl/php/powershell/nc/socat), filled in with your listener's LHOST/LPORT if given |
| `search_exploitdb` | TIER_1 |  | Search Exploit-DB for public exploits matching a search term (public API, read-only, no exploit execution) |


## Tier & role breakdown

**61 tools registered total.**

By tier (what kind of oversight each requires before running):
- Tier 1 (read-only, no approval needed): 45
- Tier 2 (reversible/logged, no approval needed): 10
- Tier 3 (notify-then-act, 3s window to cancel): 4
- Tier 4 (explicit confirmation required every call): 2

By role (security specialist taxonomy — general/ops tools have no role tag):
- defense: 6
- general: 30
- offense: 12
- practice: 13

## Current security posture

(Live snapshot at generation time — call `run_self_audit()` directly for a fresh read.)

**Status: CLEAN**

- Kill switch: not armed
- OK: Kill switch not armed (normal operating state).
- OK: .gitignore covers all known sensitive files/dirs.
- OK: No sensitive credential files are tracked in git.
- OK: 3 device(s) currently registered.
- OK: run_local_script/generate_payload/run_exploit_module use run_hardened() (env-stripped subprocess). Other subprocess call sites (recon/osint tools) use fixed argument-list commands, not shell strings.

## Test coverage

30 test files in the project root (run any individually with `python <file>`; each prints its own pass/fail count — no shared runner/CI config exists yet).

## Recent capability history

(Most recent phases from task.md — see that file for the complete history.)

- **Phase 5**: Security specialist expanded with 9 new tools, requested as "pull all cybersecurity/hacking reference... make more tools". Explicitly refused the "everyday hacking tool, do it for me" framing (autonomous/unscoped attack execution) —…
- **Phase 6**: Payload generation / exploit execution tools, requested as "add those payload tools" for "ethical hacking and to test systems in my network". Declined to build this on the first ask without scoping it — asked what specifically "payload…
- **Phase 7**: "What else can I do to make this a better AI agent" — asked for my honest assessment, then asked for everything on it. Delivered all 14 items:
- **Phase 8**: Three large external prompt templates pasted at once ("Living Mind" 3D visualizer, self-knowledge doc generator, security hardening) — asked which to prioritize first, user then said do all three. Security hardening built first…
- **Phase 4 (subagent teams)**: A large external prompt template — "restructure from coordinator + a few specialists into distinct teams" — pasted alongside a Phase 5 (self-improvement/skills) spec that depends on it. Proposed the design before writing code (per the…

## Known, deliberate scope boundaries

These are documented choices, not gaps someone forgot:
- Offensive security tools only ever act against targets in `data/authorized_targets.json` —
  there is no path to scan/exploit/generate a payload for anything not explicitly
  pre-authorized.
- `generate_payload` only ever writes a file; it never executes, transfers, or deploys it.
- `run_exploit_module` runs exactly one named module, one shot, no session chaining — a
  human decides what to do with any session it reports, not Jarvis.
- The kill switch stops tool execution; it does not revoke device tokens or kill the server
  process itself — pair it with a device revoke if the actual threat is a specific
  compromised client.
- `/upload`, `/transcribe`, and `/export/{id}` are unauthenticated HTTP endpoints by
  current design (only the WebSocket handshake enforces device tokens) — a real boundary
  worth closing if this server is ever exposed beyond a trusted LAN.
