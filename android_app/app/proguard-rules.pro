# Empty for now — isMinifyEnabled is false in build.gradle.kts (this is a thin WebView
# client, not a large codebase where R8 shrinking buys much yet). Revisit if/when this
# grows enough that release APK size actually becomes worth optimizing.
